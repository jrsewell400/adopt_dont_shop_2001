<h1>Shelter Index Page</h1>

<% @shelters.each do |shelter| %>
  <section id="shelter-<%= shelter.id %>">
    <h2><%= shelter.name %></h2>
    <%= link_to "Update Shelter", "/shelters/#{shelter.id}/edit" %>
  </section>
<% end %>

<%= link_to "New Shelter", "/shelters/new" %>